{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "io.synadia.nex.event.nexnode_lameduck",
  "title": "NexNodeLameduckSetEvent",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "The unique identifier of the nex node"
    },
    "time": {
      "type": "string",
      "format": "date-time",
      "description": "The time when the shutdown will begin"
    }
  },
  "required": [
    "id",
    "time"
  ]
}
