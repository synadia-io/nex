{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "shared",
  "title": "AgentStartWorkloadRequest",
  "type": "object",
  "properties": {
    "request": {
      "$ref": "./start-workload-request.json",
      "description": "The start workload request"
    },
    "workload_creds": {
      "$ref": "./shared-nats-connection-data.json",
      "description": "The NATS connection data for the workload"
    }
  },
  "required": [
    "request",
    "workload_creds"
  ]
}
