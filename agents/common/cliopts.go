package agentcommon

// TODO: add code that provides a standardized way of parsing the required CLI options:
//
// `{agentbin} up (path/to/node config)`
// `{agentbin} preflight (path/to/node config)`
